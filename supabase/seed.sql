-- Optional: re-enable [db.seed] in config.toml for ad-hoc local inserts.
-- Default catalog + bundled progress are applied by migration
-- 20260412210000_seed_bundled_catalog_and_progress.sql (regenerate: python3 supabase/scripts/generate_seed.py).

select 1;
